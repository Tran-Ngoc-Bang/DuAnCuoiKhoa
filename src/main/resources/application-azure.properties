# Azure Environment Configuration

# Database Configuration for Azure
spring.datasource.url=${AZURE_SQL_CONNECTION_STRING}
spring.datasource.username=${AZURE_SQL_USERNAME}
spring.datasource.password=${AZURE_SQL_PASSWORD}
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.properties.hibernate.connection.characterEncoding=utf8
spring.jpa.properties.hibernate.connection.useUnicode=true
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=never

# Server Configuration for Azure
server.port=${PORT:8080}

# File Upload Configuration
spring.servlet.multipart.max-file-size=128MB
spring.servlet.multipart.max-request-size=200MB
spring.servlet.multipart.enabled=true

# Upload Directory Configuration for Azure
app.upload.dir=/home/site/wwwroot/uploads/documents

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.com.fpoly.shared_learning_materials=INFO
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=INFO

# Disable some features for Azure
spring.jpa.show-sql=false
spring.thymeleaf.cache=true

# Security Configuration
spring.security.user.name=${ADMIN_USERNAME:admin}
spring.security.user.password=${ADMIN_PASSWORD:admin}

# JWT Configuration
jwt.secret=${JWT_SECRET:IinoHx79uRBMEZIxeOR+ERo/mN1aA6+8/zXMqoPLqpGO6atU2xLw9vGulZo2sFKRpHIwfBhxpKv3fNuhKX+Q7g==}
jwt.access-token.expiration=900000
jwt.refresh-token.expiration=604800000

# Email Configuration (disable for now)
spring.mail.enabled=false

# Payment Configuration (disable for Azure)
payment.vnpay.enabled=false
payment.qr-banking.enabled=false 